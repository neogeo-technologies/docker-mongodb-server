mongodb_data:
    image:
        neogeo/mongodb-client
    volumes:
        - /data/db
        - /data/log
    command:
        echo "MongoDB /data/db and data/log"
mongodb_server:
    image:
        neogeo/mongodb
    volumes_from:
        - mongodb_data
    ports:
        - "27017:27017"